// Creando el Type para las opciones de mostrar loading
export type ShowLoadingOptions = {
  message?: string;
  spinnerColor?: string;
  textLoadingColor?: string;
};

// Creando el Type para las opciones de ocultar el loading
export type HideLoadingOptions = {
  timeLoading?: number;
};
